export const e = Math.pow(10, -8);
export const eps = 1e-8;
export const pi = 2 * Math.asin(1.);

export const abs = (x) => {
    return x > 0? x: -x;
}

export const sqr = (x) => {
    return x * x;
}

// console.log(e, eps)